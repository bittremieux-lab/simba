# SIMBA — next steps
1. 3c: Add MCES to the selected candidate to the retrieval scoring — extend hit@1/5/20 by, for the retrieved candidates, taking the max GT MCES-based similarity to the true one, to see how close we really got even when we missed the exact match.
2. 3d: Sanity-check baseline (Gaetan) — for each test spectrum, rank the ICEBERG-predicted candidate spectra by raw cosine similarity to the real spectrum directly (no SIMBA), to quantify SIMBA's actual added value over plain spectral cosine similarity.
3. 3e: Out-of-distribution check (Gaetan) — MassSpecGym (SIMBA's training data) is narrow/metabolomics-focused, while PubChem candidates span much more of chemical space, so test-to-candidate pairs are typically far apart in a way test-to-test pairs rarely are. Take a subset of test-to-candidate pairs, compute true MCES, and compare SIMBA's predicted MAE/spearman on those against test-to-test — if it's much worse, SIMBA isn't generalizing past its training distribution and that's a ceiling on PubChem-style retrieval no matter what else we tune. Feeds directly into 4 below if confirmed.
4. 7b: Encode precursor mass properly — switch to MIST-CF-style resampling using the spectrawl implementation (lift it into a public method in metabo-depthcharge, then wire it into SIMBA's precursor-mass processing).
5. 7d: CLS-token pooling -> attention pooling (Gaetan: try this before 7c).
6. 7c: Sinusoidal m/z embeddings -> RoPE (Gaetan: a PR for this exists on depthcharge but has been open since December and isn't likely to merge soon — can dev-install that branch directly to try it now: https://github.com/wfondrie/depthcharge/pull/78).
7. 4: The weighted sampler currently targets a uniform MCES distribution in training (with a bit more weight for MCES <10, via 10 inverse-frequency-weighted bins — 4 narrow ones below MCES=10, 6 wider ones up to 40, each carrying equal total sampling mass). Check whether specific MCES ranges should have more weight than others — if 3e shows an OOD gap, weight more toward far-apart (high-MCES) pairs specifically, per Gaetan's suggestion. **New, worth checking first: the sampling weights (checked against 008_2's exact config) are computed from the cheap approximate lower-bound MCES (`max(lb_matrix, HDF5)`) in `preprocessing_official_split_max_lb_hdf5` — NOT from the exact-refined MCES that already exists on disk for this same official-split data (`data/massspecgym/preprocessing_msg_exact_mces_1020`, confirmed to be the same test-fold molecules, exact-refined wherever lb fell in [10,20]). So training (both the loss targets and the sampling weights) is using a cruder value than what's already sitting there computed. Worth checking whether `preprocessing_dir`/`preprocessing_dir_train` should just point at the exact-refined directory instead before doing any weighting redesign.
8. 2: Add more training data: NIST20 and Spectraverse, find molecules with > 10 MCES from every official val and test molecules to enrich training molecules.
9. 6: Investigate a contrastive loss paradigm (Gaetan offered to help implement it once we're ready to commit to it).
